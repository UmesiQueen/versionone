/**
 * @deprecated ServiceFeatureRow was promoted to a shared component.
 * Import `FeatureRow` from `@/components/sections/feature-row` instead.
 * This shim is kept only to avoid breaking any direct imports during the
 * transition and can be removed once verified.
 */
export {
  FeatureRow as ServiceFeatureRow,
  type FeatureRowProps as ServiceFeatureRowProps,
} from "@/components/sections/feature-row";
